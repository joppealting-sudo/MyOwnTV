name: Solcon First-Class Integration Patch

on:
  workflow_dispatch:
  push:
    branches: [feat/solcon-tv]
    paths:
      - 'tools/solcon/apply_first_class_source_integration.py'
      - 'app/src/main/res/values/strings_solcon.xml'
      - '.github/workflows/solcon-first-class-patch.yml'

permissions:
  contents: write

jobs:
  apply:
    if: github.repository == 'joppealting-sudo/MyOwnTV'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/solcon-tv
          fetch-depth: 0
      - name: Apply integration
        run: python tools/solcon/apply_first_class_source_integration.py
      - name: Verify patch shape
        run: |
          git diff --check
          git diff -- app/src/main/java app/src/main/res/values/strings_solcon.xml
      - name: Commit integration
        run: |
          if git diff --quiet; then
            echo 'No changes to commit.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/src/main/java app/src/main/res/values/strings_solcon.xml
          git commit -m 'feat: expose Solcon TV+ as an OwnTV source'
          git push origin HEAD:feat/solcon-tv
